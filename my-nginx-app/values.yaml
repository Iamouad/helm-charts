replicaCount: 1

image:
  repository: nginx-custom
  pullPolicy: IfNotPresent
  tag: "0.0.1"

service:
  type: ClusterIP
  port: 80
  containerPort: 80

ingress:
  enabled: true
  className: "nginx"
  annotations: {}
  hosts: []
  tls: []

resources:
  limits:
    cpu: 100m
    memory: 128Mi
  requests:
    cpu: 100m
    memory: 128Mi

htmlContent:
  message: ""